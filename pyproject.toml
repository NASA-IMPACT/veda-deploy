[project]
name = "veda-deploy"
version = "0.1.0"
description = "This is a virtual repo that's created to deploy all the VEDA services"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "boto3==1.26.62",
  "requests==2.28.2"
]

[dependency-groups]
dev = [
  "pre-commit==4.6.2"
]

[tool.uv]
default-groups = ["dev"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # Pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "PTH",  # flake8-use-pathlib
    "RET",  # flake8-return
    "TID",  # flake8-tidy-imports
    "TC",   # flake8-type-checking
    "ISC",  # flake8-implicit-str-concat
    "ICN",  # flake8-import-conventions
    "BLE",  # flake8-blind-except
]
ignore = [
  "BLE001",  # Blind Exceptions
]

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = "dynamic"

[tool.markdownlint-cli2.config]
MD013 = false
MD033 = { allowed_elements = ["a", "b", "details", "summary"] }
