name: CI/CD 🚀

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      environment:
        type: environment
        required: true
        description: Environment to deploy to

concurrency:
  group: ${{ inputs.environment }}
  cancel-in-progress: true

run-name: Deploy to ${{ inputs.environment }} environment by @${{ github.actor }}

jobs:
  export-env-vars:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    outputs:
      DEPLOY_FEATURES_API: ${{ steps.init.outputs.DEPLOY_FEATURES_API }}
      DEPLOY_DATA_AIRFLOW: ${{ steps.init.outputs.DEPLOY_DATA_AIRFLOW }}
      DEPLOY_MONITORING: ${{ steps.init.outputs.DEPLOY_MONITORING }}
      DEPLOY_AUTH: ${{ steps.init.outputs.DEPLOY_AUTH }}
      DEPLOY_BACKEND: ${{ steps.init.outputs.DEPLOY_BACKEND }}

    steps:        
      - name: Environment variables to output 
        id: init
        run: |
          echo "DEPLOY_FEATURES_API=${{ vars.DEPLOY_FEATURES_API }}" >> $GITHUB_OUTPUT
          echo "DEPLOY_DATA_AIRFLOW=${{ vars.DEPLOY_DATA_AIRFLOW }}" >> $GITHUB_OUTPUT
          echo "DEPLOY_MONITORING=${{ vars.DEPLOY_MONITORING }}" >> $GITHUB_OUTPUT
          echo "DEPLOY_AUTH=${{ vars.DEPLOY_AUTH }}" >> $GITHUB_OUTPUT
          echo "DEPLOY_BACKEND=${{ vars.DEPLOY_BACKEND }}" >> $GITHUB_OUTPUT

  deploy-veda-auth:
    name: Deploy VEDA auth 🔐
    runs-on: ubuntu-latest
    env:
      DIRECTORY: veda-auth
      ENVIRONMENT: ${{ github.event.inputs.environment }}
    environment: ${{ github.event.inputs.environment }}
    needs: [export-env-vars]
    if: needs.export-env-vars.outputs.DEPLOY_AUTH

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: "true"
          submodules: "false"

      - name: Checkout veda-auth submodule
        uses: actions/checkout@v4
        with:
          repository: "NASA-IMPACT/${{ env.DIRECTORY }}"
          path: ${{ env.DIRECTORY }}
          ref: ${{ vars.VEDA_AUTH_GIT_REF || 'main'}}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          role-session-name: "gh-${{ env.ENVIRONMENT }}-auth-deployment"
          aws-region: "us-west-2"

      - name: Run auth deployment
        id: deploy_auth_stack
        uses: "./veda-auth/.github/actions/cdk-deploy"
        with:
          dir: "${{ env.DIRECTORY }}"
          env_aws_secret_name: ${{ vars.DEPLOYMENT_ENV_SECRET_NAME }}

      - name: Get Auth Stack Name
        id: get_auth_stack
        shell: bash
        run: |
          stack=$(jq 'keys_unsorted[0]' ${HOME}/cdk-outputs.json)
          echo "auth_stackname=$stack" >> $GITHUB_OUTPUT

    outputs:
      auth_stack_name: ${{ steps.get_auth_stack.outputs.auth_stackname }}

  deploy-veda-backend:
    name: Deploy VEDA backend ⚙️
    runs-on: ubuntu-latest
    env:
      DIRECTORY: veda-backend
      ENVIRONMENT: ${{ github.event.inputs.environment }}
    needs: [deploy-veda-auth, export-env-vars]
    if: needs.export-env-vars.outputs.DEPLOY_BACKEND
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: "true"
          submodules: "false"

      - name: Checkout veda-backend submodule
        uses: actions/checkout@v4
        with:
          repository: "NASA-IMPACT/${{ env.DIRECTORY }}"
          path: ${{ env.DIRECTORY }}
          ref: ${{ vars.VEDA_BACKEND_GIT_REF || 'main'}}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          role-session-name: "gh-${{ env.ENVIRONMENT }}-backend-deployment"
          aws-region: "us-west-2"

      - name: Run deployment
        uses: "./veda-backend/.github/actions/cdk-deploy"
        id: deploy_backend_stack
        with:
          env_aws_secret_name: ${{ vars.DEPLOYMENT_ENV_SECRET_NAME }}
          auth_stack_name: "${{ needs.deploy-veda-auth.outputs.auth_stack_name }}"
          dir: "${{ env.DIRECTORY }}"
          script_path: "${{ github.workspace }}/scripts/generate_env_file.py"

      - name: Get Backend Stack Name
        id: get_backend_stack
        shell: bash
        run: |
          stack=$(jq 'keys_unsorted[0]' ${HOME}/cdk-outputs.json)
          echo "backend_stackname=$stack" >> $GITHUB_OUTPUT

    outputs:
      backend_stack_name: ${{ steps.get_backend_stack.outputs.backend_stackname }}

  deploy-veda-data-airflow:
    name: deploy VEDA data airflow 🍃
    runs-on: ubuntu-latest
    env:
      DIRECTORY: veda-data-airflow
      AWS_REGION: "us-west-2"
      ENVIRONMENT: ${{ github.event.inputs.environment }}
    needs: [deploy-veda-auth, deploy-veda-backend, export-env-vars]
    if: needs.export-env-vars.outputs.DEPLOY_DATA_AIRFLOW
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: "true"
          submodules: "false"

      - name: Checkout veda-data-airflow submodule
        uses: actions/checkout@v4
        with:
          repository: "NASA-IMPACT/${{ env.DIRECTORY }}"
          path: ${{ env.DIRECTORY }}
          ref: ${{ vars.VEDA_DATA_AIRFLOW_GIT_REF || 'main'}}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          role-session-name: "gh-${{ env.ENVIRONMENT }}-airflow-deployment"
          aws-region: "${{ env.AWS_REGION }}"

      - name: Run deployment
        uses: "./veda-data-airflow/.github/actions/terraform-deploy"
        with:
          env-file: ".env"
          env_aws_secret_name: ${{ vars.DEPLOYMENT_ENV_SECRET_NAME }}
          dir: "${{ env.DIRECTORY }}"
          script_path: "${{ github.workspace }}/scripts/generate_env_file.py"
          backend_stack_name: "${{ needs.deploy-veda-backend.outputs.backend_stack_name }}"
          auth_stack_name: "${{ needs.deploy-veda-auth.outputs.auth_stack_name }}"

  deploy-veda-features-api:
    name: Deploy VEDA features-api 🗺️
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    needs: [export-env-vars]
    if: needs.export-env-vars.outputs.DEPLOY_FEATURES_API
    env:
      DIRECTORY: veda-features-api-cdk
      ENVIRONMENT: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: "true"
          submodules: "false"

      - name: Checkout veda-features-api submodule
        uses: actions/checkout@v4
        with:
          repository: "NASA-IMPACT/${{ env.DIRECTORY }}"
          path: ${{ env.DIRECTORY }}
          ref: ${{ vars.VEDA_FEATURES_API_GIT_REF || 'main'}}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          role-session-name: "gh-${{ env.ENVIRONMENT }}-features-deployment"
          aws-region: "us-west-2"

      - name: Run deployment
        uses: "./veda-features-api-cdk/.github/actions/cdk-deploy"
        id: deploy_features_stack
        with:
          env_aws_secret_name: ${{ vars.DEPLOYMENT_ENV_SECRET_NAME }}
          dir: "${{ env.DIRECTORY }}"
          script_path: "${{ github.workspace }}/scripts/generate_env_file.py"

      - name: Get Features API Stack Name
        id: get_features_api_stack
        shell: bash
        run: |
          stack=$(jq 'keys_unsorted[0]' ${HOME}/cdk-outputs.json)
          echo "features_api_stackname=$stack" >> $GITHUB_OUTPUT

    outputs:
      features_api_stack_name: ${{ steps.get_features_api_stack.outputs.features_api_stackname }}

  deploy-veda-monitoring:
    name: deploy VEDA monitoring 👁️
    runs-on: ubuntu-latest
    env:
      DIRECTORY: veda-monitoring
      AWS_REGION: "us-west-2"
      ENVIRONMENT: ${{ github.event.inputs.environment }}
      GH_PAT_CHECK: ${{ secrets.GH_PAT }}
    needs: [deploy-veda-backend, export-env-vars]
    if: needs.export-env-vars.outputs.DEPLOY_MONITORING
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        if: ${{ env.GH_PAT_CHECK != '' }}
        uses: actions/checkout@v4
        with:
          lfs: "true"
          submodules: "false"

      - name: Checkout veda-monitoring "submodule"
        if: ${{ env.GH_PAT_CHECK != '' }}
        uses: actions/checkout@v4
        with:
          repository: "NASA-IMPACT/${{ env.DIRECTORY }}"
          path: ${{ env.DIRECTORY }}
          ref: ${{ vars.VEDA_MONITORING_GIT_REF || 'main' }}
          token: ${{ secrets.GH_PAT }}

      - name: Configure AWS Credentials
        if: ${{ env.GH_PAT_CHECK != '' }}
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          role-session-name: "gh-${{ env.ENVIRONMENT }}-monitoring-deployment"
          aws-region: "${{ env.AWS_REGION }}"
          
      - name: Run deployment
        if: ${{ env.GH_PAT_CHECK != '' }}
        uses: "./veda-monitoring/.github/actions/cdk-deploy"
        with:
          env_aws_secret_name: ${{ vars.DEPLOYMENT_ENV_SECRET_NAME }}
          dir: "${{ env.DIRECTORY }}"
          script_path: "${{ github.workspace }}/scripts/generate_env_file.py"

  test-deployment:
    name: Test Deployment 👨🏻‍🔬
    runs-on: ubuntu-latest
    needs: [ deploy-veda-backend ]
    env:
      DIRECTORY: integration_test
      ENVIRONMENT: ${{ github.event.inputs.environment }}
      AWS_DEFAULT_REGION: us-west-2
    environment: ${{ github.event.inputs.environment }}
    steps:
      - uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pip"
          cache-dependency-path: |
            ${{ github.workspace }}/${{ env.DIRECTORY }}/requirements.txt

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          role-session-name: "gh-${{ env.ENVIRONMENT }}-integration-test"
          aws-region: "${{ env.AWS_DEFAULT_REGION }}"

      - name: Install python dependencies
        shell: bash
        working-directory: ${{ env.DIRECTORY }}
        run: |
          pip install -r requirements.txt
          python "${{ github.workspace }}/scripts/generate_env_file.py" --secret-id ${{ vars.DEPLOYMENT_ENV_SECRET_NAME }} --stack-names "${{ needs.deploy-veda-backend.outputs.backend_stack_name }}"

      - name: Integration test
        shell: bash
        working-directory: ${{ env.DIRECTORY }}
        run: |
          pytest .
